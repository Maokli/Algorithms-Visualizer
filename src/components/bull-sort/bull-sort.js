const BullSort = ({array, startIndex, colorFactor}) => {
    
    return ( 
        <VisualizationBoard array={array} colorFactor={colorFactor} startIndex={startIndex}/>
    );
}
 
export default BullSort;